---
import Head from '@components/infra/Head.astro';
import TheHeader from '@components/TheHeader.svelte';

export const prerender = false;

interface Props {
	title?: string;
	page?: 'home' | 'default';
}

const { title, page = 'default' } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<Head {title} />
	</head>
	<body>
		<TheHeader {page} client:only="svelte" />
		<slot />
	</body>
</html>
